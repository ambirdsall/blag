<% require 'pry' %>
# ambirdsall

<%
articles_by_topic = blog.articles.group_by {|a| a.data.topic}
articles_by_topic.each do |topic, list|
  topic_name = topic.titleize
%>
### <%= topic_name %>

<% list.each do |article| %>
  * <%= article.title %>
<% end %>

<% end %>
